page.visual_effect :SlideDown, 'menu'

page.delay(4.4) do
  page.visual_effect :Pulsate, 'menu'
end

page.delay(5.0) do
 page.closemenu
end


page.delay(5.6) do
  page.visual_effect :Highlight, 'logo', :duration => 1
end

